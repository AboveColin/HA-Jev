# Pin what the harness pins. A mismatch here shows up as unrelated async failures.
pytest-homeassistant-custom-component==0.13.365
pytest==9.0.3
pytest-asyncio==1.4.0
pyyaml

# The same version manifest.json pins, which is what Home Assistant installs for a
# user. tests/test_manifest.py fails if these two ever disagree.
jevclient==1.2.0

# The conversation component's own requirements. Home Assistant installs these for a
# user the moment a conversation agent exists; a test environment gets nothing
# unless it is asked, and the failure is an ImportError at collection rather than a
# failing assertion. The versions are exactly what
# homeassistant/components/conversation/manifest.json pins, so the suite runs against
# what a user runs. tests/test_manifest.py fails if they ever drift apart.
gazetteer-matcher==1.1.0
hassil==3.12.0
home-assistant-intents==2026.8.28

# The AI Task platform's chain. ai_task/task.py imports the camera component at
# module level, and camera pins this. Without it every test that sets up an entry
# fails at platform import with ModuleNotFoundError: No module named 'turbojpeg'.
# The import alone needs no libturbojpeg on the machine: PyTurboJPEG looks for the
# shared library when a TurboJPEG object is built, which nothing here does.
# tests/test_manifest.py imports the chain, so a new pin in a later Home Assistant
# fails there rather than in every other module.
PyTurboJPEG==1.8.3
