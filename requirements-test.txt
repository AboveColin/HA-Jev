# Pin what the harness pins. A mismatch here shows up as unrelated async failures.
pytest-homeassistant-custom-component==0.13.365
pytest==9.0.3
pytest-asyncio==1.4.0
pyyaml

# The same version manifest.json pins, which is what Home Assistant installs for a
# user. tests/test_manifest.py fails if these two ever disagree.
jevclient==1.1.0
