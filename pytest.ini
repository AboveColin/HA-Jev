[pytest]
testpaths = tests
# tests/live is a script that drives a real instance and spends real tokens. It must
# never be collected: pytest imports a matching file and runs it top to bottom.
norecursedirs = tests/live
asyncio_mode = auto
addopts = --cov=custom_components.jev --cov-report=term-missing
